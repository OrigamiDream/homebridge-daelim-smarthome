import {Device, DeviceType} from "../interfaces/smart-elife-config";

export const EXTERIOR_ELEVATOR_DEVICE: Device = {
    displayName: "외부 엘리베이터",
    name: "엘리베이터",
    deviceType: DeviceType.ELEVATOR,
    deviceId: "CMF990100",
    disabled: false,
};
