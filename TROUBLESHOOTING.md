# 문제 해결

다양한 스펙트럼의 사용자들이 이 소프트웨어를 사용하다 보면 개발자가 예상치 못한 부분에 대하여 때로는 문제나 어려움을 겪을 수도 있습니다.<br>
이를 해결하기 위하여 본 문서의 절차를 따라 이슈를 작성해주시거나 메일로 문의해주세요.
<br>
<br>

## homebridge-ui를 사용하는 사용자의 경우
1. 해당 문제가 발생했을 때, 어떠한 절차를 거쳐야 문제를 재현할 수 있는지 최대한 설명해주시면 큰 도움이 됩니다.
2. 로그 다운로드 및 첨부
   1. homebridge-ui 관리 패널에 접속
   2. 우측 상단 `로그 보기` 버튼 클릭
   3. 우측 상단 `다운로드` 버튼 클릭 (사용자 환경에 따라 개인정보 등이 포함될 수 있습니다.)
   4. 다운로드된 `homebridge.log.txt` 파일을 적절한 검열을 거쳐 [새 이슈 작성](https://github.com/OrigamiDream/homebridge-daelim-smarthome/issues/new)을 해 주시거나 개발자의 [메일](mailto:hello@origamidream.me)로 보내주세요.

<br>
<br>

## Terminal에서 직접적으로 사용하는 사용자의 경우
1. 해당 문제가 발생했을 때, 어떠한 절차를 거쳐야 문제를 재현할 수 있는지 최대한 설명해주시면 큰 도움이 됩니다.
2. 로그 다운로드
   1. Terminal 에서 `~/.homebridge` 혹은 사용자 설정된 Homebridge 디렉토리로 이동
   2. `homebridge.log` 파일을 적절한 검열을 거쳐 [새 이슈 작성](https://github.com/OrigamiDream/homebridge-daelim-smarthome/issues/new)을 해 주시거나 개발자의 [메일](mailto:hello@origamidream.me)로 보내주세요.

<br>
<br>

## homebridge.log 파일에서 오류를 분간하는 방법

homebridge-ui 패널의 콘솔에서 관찰하는 경우, 오류가 발생한 기록은 보통 `빨간색`으로 표시됩니다.<br>
혹은 `TypeError`, `ReferenceError` 등의 문구가 포함되어 오류임을 구분할 수 있습니다.<br><br>
해당 부분으로부터 **위, 아래로 약 30~50줄 사이를 포함한 텍스트**에서 개인정보를 최대한 가린 후 위 절차를 따라 주세요.

<br>
<br>

## 세대현관 실시간 영상이 뜨지 않는 경우

`세대현관 실시간 영상 (One Pass)`을 켰는데도 홈 앱이 카메라를 열지 못하고, 로그에 아래와 같은 줄이 남는 경우입니다.

```
[외부 세대현관 초인종] Live view unavailable: ...
```

### 1. 폰의 One Pass 앱이 세대현관을 보고 있는지 확인하세요

인터폰 회선은 한 번에 한 통화만 가능합니다. 폰 앱에서 세대현관 모니터링 중이거나 통화 중이면 플러그인은 영상을 가져올 수 없습니다. 앱에서 통화를 종료한 뒤 다시 시도하세요. 이 경우 로그에 `486`, `480` 같은 응답 코드가 함께 남습니다.

### 2. 공유기가 SIP 트래픽을 막고 있는지 확인하세요

실시간 영상은 단지 서버의 5061 포트(SIP over TLS)로 연결합니다. 일부 공유기는 이 포트를 기본적으로 차단합니다. Homebridge가 돌아가는 기기에서 다음 명령으로 확인할 수 있으며, `succeeded`가 나오지 않으면 공유기 설정 문제입니다.

```
nc -z -v -w 5 <단지 서버 주소> 5061
```

SK브로드밴드 공유기(Humax H633G) 기준으로는 관리 페이지 → 고급설정 → NAT/라우터 → VPN → ALG의 **`SIP 사용`을 활성화**하면 통과합니다. 이 항목은 "SIP ALG를 적용한다"가 아니라 "SIP 트래픽을 통과시킨다"는 뜻이라 이름과 동작이 직관과 반대이니 주의하세요. 다른 제조사 공유기에도 `SIP ALG`, `SIP 통과`, `VoIP 통과` 등의 이름으로 같은 설정이 있습니다.

### 3. 동·호수가 제대로 잡혔는지 확인하세요

로그에 `One Pass sign-in failed (Fail.notFound)`가 남으면 단지 코드나 동·호수가 맞지 않는 경우입니다. 플러그인 설정의 `고급 설정`에서 `동`, `호`를 네 자리(예: `0103`, `0803`)로 직접 입력해 보세요.

### 4. 인증서 지문이 다르다는 오류가 나는 경우

인터폰 서버는 공인 인증서를 쓰지 않고 IP로 접속하기 때문에, 플러그인은 폰 앱과 마찬가지로 **인증서 지문을 직접 대조해서** 서버를 확인합니다. 지금까지 확인된 모든 단지가 같은 인증서를 쓰므로 이 값은 플러그인에 내장되어 있습니다.

로그에 `The One Pass PBX presented an unexpected certificate: ...`가 남으면 단지 서버가 다른 인증서를 쓰고 있거나, 중간에서 연결이 가로채이고 있다는 뜻입니다. 먼저 공용 와이파이나 VPN을 쓰고 있지 않은지 확인하세요.

정상적인 회선인데도 계속 같은 오류가 난다면, 신뢰할 수 있는 네트워크에서 아래 명령으로 지문을 확인한 뒤 `고급 설정`의 `인터폰 서버 인증서 지문`에 그 값을 입력하세요. 이 값을 입력하면 내장된 지문 대신 그 지문으로 검증합니다.

```
openssl s_client -connect <단지 서버 주소>:5061 </dev/null 2>/dev/null | openssl x509 -noout -fingerprint -sha256
```